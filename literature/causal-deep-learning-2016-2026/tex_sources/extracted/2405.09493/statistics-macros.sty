% Macros for lecture notes

% Symbol formats
\newcommand{\mc}[1]{\mathcal{#1}}
\newcommand{\mbf}[1]{\mathbf{#1}}
\newcommand{\mrm}[1]{\mathrm{#1}}
\newcommand{\mbi}[1]{\boldsymbol{#1}} % Bold and italic (math bold italic)

% Norms for convenience, along with sizes
\newcommand{\norm}[1]{\left\|{#1}\right\|} % A norm with 1 argument
\newcommand{\lone}[1]{\norm{#1}_1} % l1 norm
\newcommand{\ltwo}[1]{\norm{#1}_2} % l2 norm
\newcommand{\linf}[1]{\norm{#1}_\infty} % l-infinity norm
\newcommand{\dnorm}[1]{\norm{#1}_*} % Dual norm
\newcommand{\lfro}[1]{\left\|{#1}\right\|_{\rm Fr}} % Frobenius norm
\newcommand{\matrixnorm}[1]{\left|\!\left|\!\left|{#1}
  \right|\!\right|\!\right|} % Matrix norm with three bars
\newcommand{\normbigg}[1]{\bigg\|{#1}\bigg\|} % A norm with 1 argument and bigg
                                              % brackets.
\newcommand{\normbig}[1]{\big\|{#1}\big\|} % A norm with 1 argument and big
                                           % brackets.
\newcommand{\lonebigg}[1]{\normbigg{#1}_1} % l1 norm
\newcommand{\ltwobigg}[1]{\normbigg{#1}_2} % l2 norm
\newcommand{\ltwobig}[1]{\normbig{#1}_2} % l2 norm
\newcommand{\linfbigg}[1]{\normbigg{#1}_\infty} % l-infinity norm
\newcommand{\norms}[1]{\|{#1}\|} % A norm with 1 argument and normal (small) brackets.
\newcommand{\normBig}[1]{\Big\|{#1}\Big\|} % A norm with 1 argument and Big brackets
\newcommand{\dnormbigg}[1]{\bigg\|{#1}\bigg\|_*} % A dual norm with 1
                                                 % argument and bigg
                                                 % brackets.
\newcommand{\ltwoBig}[1]{\normBig{#1}_2} % l2 norm


\newcommand{\lones}[1]{\norms{#1}_1} % l1 norm with small brackets
\newcommand{\ltwos}[1]{\norms{#1}_2} % l2 norm with small brackets
\newcommand{\linfs}[1]{\norms{#1}_\infty} % l-infinity norm with small brackets
\newcommand{\matrixnormbigg}[1]{\bigg|\!\bigg|\!\bigg|{#1}
  \bigg|\!\bigg|\!\bigg|} % Matrix norm with three bars
\newcommand{\matrixnormBig}[1]{\Big|\!\Big|\!\Big|{#1}
  \Big|\!\Big|\!\Big|} % Matrix norm with three bars
\newcommand{\matrixnorms}[1]{|\!|\!|{#1}|\!|\!|} % Small matrix norm
\newcommand{\lfrobigg}[1]{\normbigg{#1}_{\rm Fr}}
\newcommand{\lfros}[1]{\norms{#1}_{\rm Fr}}
\newcommand{\lfrobig}[1]{\normbig{#1}_{\rm Fr}}
\newcommand{\opnorm}[1]{\norm{#1}_{\rm op}} 

\newcommand{\defeq}{:=}
\newcommand{\eqdef}{=:}
\newcommand{\what}[1]{\widehat{#1}} % Wide hat
\newcommand{\wt}[1]{\widetilde{#1}} % Wide tilde
\newcommand{\wb}[1]{\overline{#1}} % Wide line
\newcommand{\equalto}{=}

\newcommand{\half}{\frac{1}{2}}
\newcommand{\indic}[1]{\mbf{1}\left\{#1\right\}} % Indicator function
\newcommand{\bigindic}[1]{\mbf{I}_{#1}} % Indicator function

\newcommand{\reals}{\mathbb{R}} % Real number symbol
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}} % Complex numbers
\newcommand{\integers}{\mathbb{Z}} % Integer symbol
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\rationals}{\mathbb{Q}} % Rational numbers
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\naturals}{\mathbb{N}} % Natural numbers
\newcommand{\N}{\mathbb{N}}
\newcommand{\B}{\mathbb{B}}
\newcommand{\sphere}{\mathbb{S}}

%%% New version of \caption puts things in smaller type, single-spaced
%%% and indents them to set them off more from the text.
\makeatletter
\long\def\@makecaption#1#2{
  \vskip 0.8ex
  \setbox\@tempboxa\hbox{\small {\bf #1:} #2}
  \parindent 1.5em  %% How can we use the global value of this???
  \dimen0=\hsize
  \advance\dimen0 by -3em
  \ifdim \wd\@tempboxa >\dimen0
  \hbox to \hsize{
    \parindent 0em
    \hfil
    \parbox{\dimen0}{\def\baselinestretch{0.96}\small
      {\bf #1.} #2
      %%\unhbox\@tempboxa
    }
    \hfil}
  \else \hbox to \hsize{\hfil \box\@tempboxa \hfil}
  \fi
}
\makeatother

% Symbols
\newcommand{\cf}{{\it cf.}}
\newcommand{\eg}{{\it e.g.}}
\newcommand{\ie}{{\it i.e.}}
\newcommand{\etc}{{\it etc.}}

\newcommand{\<}{\left\langle} % Angle brackets
\renewcommand{\>}{\right\rangle}

\renewcommand{\iff}{\Leftrightarrow}
\renewcommand{\choose}[2]{\binom{#1}{#2}}
\newcommand{\chooses}[2]{{}_{#1}C_{#2}}
\newcommand{\openleft}[2]{\left({#1},{#2}\right]} % Interval open on left
\newcommand{\openright}[2]{\left[{#1},{#2}\right)} % Interval open on right

%%%% Probability symbols and associated distances %%%%

\newcommand{\E}{\mathbb{E}} % Expectation symbol
\renewcommand{\P}{\mathbb{P}} % Probability symbo
\newcommand{\var}{{\rm Var}} % Variance
\newcommand{\Var}{{\rm Var}} % Variance
\newcommand{\cov}{{\rm Cov}} % Covariance
\newcommand{\corr}{\mathop{\rm Corr}} % Correlation
\newcommand{\simiid}{\stackrel{\rm iid}{\sim}}
\newcommand{\tvnorm}[1]{\norm{#1}_{\rm TV}}
\newcommand{\tvnormbigg}[1]{\normbigg{#1}_{\rm TV}}
\newcommand{\tvnorms}[1]{\norms{#1}_{\rm TV}}
\newcommand{\dkl}[2]{D_{\rm kl}\left({#1} |\!| {#2}\right)}  % KL-divergence
\newcommand{\dkls}[2]{D_{\rm kl}({#1} |\!| {#2})}  % Small KL-divergence
\newcommand{\dchi}[2]{D_{\chi^2}({#1} |\!| {#2})}  % Small KL-divergence
\newcommand{\fdiv}[2]{D_f\left({#1} |\!| {#2} \right)}  % f-divergence
\newcommand{\fdivs}[2]{D_f({#1} |\!| {#2})}  % f-divergence with small parens
\newcommand{\fdivone}[3]{D_{#3}\left({#1} |\!| {#2} \right)}  % f-divergence
\newcommand{\dhel}{d_{\rm hel}}
\newcommand{\risk}{\mc{R}} % Risk measure
\newcommand{\emp}{{\what{P}_n}} % empirical measure
\newcommand{\cvar}[1]{{\rm CVaR_{#1}}}
\newcommand{\eqd}{\stackrel{d}{=}} % Convergence a.s.*
\newcommand{\ind}{\perp\!\!\!\!\perp} 

% Convergence of random variables
\newcommand{\cd}{\stackrel{d}{\rightsquigarrow}} % Convergences in distribution
\newcommand{\cas}{\stackrel{a.s.}{\rightarrow}} % Convergence a.s.
\newcommand{\casstar}{\stackrel{a.s.*}{\rightarrow}} % Convergence a.s.*
\newcommand{\cp}{\stackrel{p}{\rightarrow}} % Convergence in probability
\newcommand{\cpstar}{\stackrel{P^*}{\rightarrow}} % Convergence in outer probability
\newcommand{\cau}{\stackrel{a.u.}{\rightarrow}}

%%%% Convenient mathematical symbols and operators %%%%

\providecommand{\argmax}{\mathop{\rm argmax}} % Defining math symbols
\providecommand{\argmin}{\mathop{\rm argmin}}
\providecommand{\dom}{\mathop{\rm dom}}
\providecommand{\diag}{\mathop{\rm diag}}
\newcommand{\rank}{\mathop{\rm rank}}
\providecommand{\tr}{\mathop{\rm tr}}
\providecommand{\abs}{\mathop{\rm abs}}
\providecommand{\card}{\mathop{\rm card}}
\providecommand{\sign}{\mathop{\rm sign}}
\providecommand{\sgn}{\mathop{\rm sgn}}
\providecommand{\cl}{\mathop{\rm cl}}  % closure of a set
\providecommand{\interior}{\mathop{\rm int} \hspace{2pt}}  % interior of a set
\providecommand{\diam}{\mathop{\rm diam}} % dimaeter of a set
\providecommand{\bd}{\mathop{\rm bd}} % boundary of a set
\providecommand{\conv}{\mathop{\rm Conv}}
\providecommand{\relint}{\mathop{\rm relint}}
\newcommand{\supp}{\mathop{\rm supp}} % support of a function
\newcommand{\dist}{\mathop{\rm dist}} % Distance
\newcommand{\dhaus}{d_{\rm haus}} % Hausdorff distance between sets
\newcommand{\dinclude}{d_{\subset}} % Deviation distance between sets
\newcommand{\aff}{\mathop{\rm Aff}} % Affine hull

\newcommand{\hinge}[1]{\left({#1}\right)_+} % Max of argument and zero
\newcommand{\hinges}[1]{({#1})_+}
\newcommand{\hingebigg}[1]{\bigg({#1}\bigg)_+} % Max of argument and zero
\newcommand{\hingeBig}[1]{\Big({#1}\Big)_+} % Max of argument and zero

\newcommand{\opt}{^\star}

\providecommand{\minimize}{\mathop{\rm minimize}}
\providecommand{\maximize}{\mathop{\rm maximize}}
\providecommand{\subjectto}{\mathop{\rm subject\;to}}
\newcommand{\ceil}[1]{\left\lceil{#1}\right\rceil}
\newcommand{\floor}[1]{\left\lfloor{#1}\right\rfloor}

\newcommand{\figref}[1]{Fig.~\ref{#1}}

% Proof environments
% The Theorems are numbered consecutively
% Lemmas are numbered by section, and observations, claims, facts, and
% assumptions take their numbering. Propositions and definitions have their
% own numbering by section.
\newtheorem{theorem}{Theorem}
\newtheorem{corollary}{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{definition}{Definition}
\newtheorem{lemma}{Lemma}
\newtheorem{observation}[lemma]{Observation}
\newtheorem{claim}[lemma]{Claim}
\newtheorem{fact}{Fact}
\newtheorem{assumption}{Assumption}
\newtheorem{conjecture}{Conjecture}

% \newenvironment{assumptionprime}
% {\addtocounter{assumption}{-1}
% \renewcommand{\theassumption}{\Alph{assumption}$~'$}
%   % \neutralize{assumption}\phantomsection
%   \begin{assumption}}
%   {\end{assumption}}

% Makes equation numbers have (1.1) style
% \numberwithin{equation}{section}
% \numberwithin{equation}{subsection}


\renewenvironment{proof}{\noindent{\bf Proof}\hspace*{1em}}{\qed\bigskip\\}

\newenvironment{proof-of-claim}{\noindent{\bf Proof of Claim}
  \hspace*{1em}}{\qed\bigskip\\}

\newenvironment{proof-sketch}{\noindent{\bf Sketch of Proof}
  \hspace*{1em}}{\qed\bigskip\\}
\newenvironment{proof-idea}{\noindent{\bf Proof Idea}
  \hspace*{1em}}{\qed\bigskip\\}
\newenvironment{proof-of-lemma}[1][{}]{\noindent{\bf Proof of Lemma {#1}}
  \hspace*{1em}}{\qed\bigskip\\}
  \newenvironment{proof-of-corollary}[1][{}]{\noindent{\bf Proof of Corollary {#1}}
  \hspace*{1em}}{\qed\bigskip\\}
\newenvironment{proof-of-proposition}[1][{}]{\noindent{\bf
    Proof of Proposition {#1}}
  \hspace*{1em}}{\qed\bigskip\\}
\newenvironment{proof-of-theorem}[1][{}]{\noindent{\bf Proof of Theorem {#1}}
  \hspace*{1em}}{\qed\bigskip\\}
\newenvironment{inner-proof}{\noindent{\bf Proof}\hspace{1em}}{
  $\bigtriangledown$\medskip\\}
%% \newenvironment{proof-of-lemma}[1][{}]{\noindent{\bf Proof of Lemma {#1}}
%%   \hspace*{1em}\renewcommand{\qedsymbol}{$\bigtriangledown$}}{\qed\bigskip\\}
\newenvironment{proof-attempt}{\noindent{\bf Proof Attempt}
  \hspace*{1em}}{\qed\bigskip\\}
\newenvironment{proofof}[1]{\noindent{\bf Proof} of {#1}:
  \hspace*{1em}}{\qed\bigskip\\}

\newcounter{example}

\newenvironment{example}[1][]{
  \refstepcounter{example}
  \ifthenelse{\isempty{#1}}{%
    % There is no text to display in the example
    \noindent \textbf{Example \theexample:}\hspace*{.05em}
  }{%
    \noindent \textbf{Example \theexample} ({#1})\textbf{:}\hspace*{.05em}
  }
}{%
  $\diamond$ \bigskip
}
\newenvironment{example_num}[2]{
  \refstepcounter{example}
  \ifthenelse{\isempty{#1}}{%
    % There is no text to display in the example
    \noindent \textbf{Example #2:}\hspace*{.05em}
  }{%
    \noindent \textbf{Example #2} ({#1})\textbf{:}\hspace*{.05em}
  }
}{%
  $\diamond$ \bigskip
}

\newenvironment{example*}[1][]{
  \ifthenelse{\isempty{#1}}{%
    % There is no text to display in the example
    \noindent \textbf{Example:}\hspace*{.05em}
  }{%
    \noindent \textbf{Example} ({#1})\textbf{:}\hspace*{.05em}
  }
}{%
  $\diamond$ \bigskip
}

\newcounter{remark}

\newenvironment{remark}[1][]{
  \refstepcounter{remark}
  \ifthenelse{\isempty{#1}}{%
    % There is no text to display in the example
    \noindent \textbf{Remark \theremark:}\hspace*{.05em}
  }{%
    \noindent \textbf{Remark \theremark} ({#1})\textbf{:}\hspace*{.05em}
  }
}{%
  $\diamondsuit$ \bigskip
}

\newenvironment{remark*}[1][]{
  \ifthenelse{\isempty{#1}}{%
    % There is no text to display in the example
    \noindent \textbf{Remark:}\hspace*{.05em}
  }{%
    \noindent \textbf{Remark} ({#1})\textbf{:}\hspace*{.05em}
  }
}{%
  $\diamondsuit$ \bigskip
}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%% Notation for RobustSAA Paper %%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%



\renewcommand{\theassumption}{\Alph{assumption}}

\newcommand{\zlow}{Z_\star}
\newcommand{\fconp}{{f^*}'}
\newcommand{\image}{\mathop{\rm Im}}
\newcommand{\taylrem}[2]{\mathsf{r}_{#1,#2}}

\newcommand{\stochprocess}{\xi}

\newcommand{\obj}{\loss(\theta; \statval)}  % Objective
\newcommand{\obji}{\loss(\theta; \statval_i)}
\newcommand{\loss}{\ell}  % Instantaneous loss function \loss
\newcommand{\tol}{\rho}  % Tolerance for deviation
\newcommand{\xdomain}{\Theta}

\newcommand{\statrv}[1][]{%  % Statistical random variable
  \ifthenelse{\isempty{#1}}{%
    X
  }{%
    X_{#1}
  }
}

\newcommand{\likerat}{L} % Likelihood ratio
% \newcommand{\rp}{f^{*'} \left(\frac{1}{n\lambda_n}(Z_i -
%     \eta_n)\right)}
\newcommand{\fclass}{\mc{H}}
\newcommand{\fspace}{\ell^\infty(\fclass)}
\newcommand{\ltwop}[2]{\norm{#1}_{L^2(#2)}} % l2, P norm
\newcommand{\ave}{\frac{1}{n}\sum_{i=1}^n}
\newcommand{\event}{\mc{E}}

\newcommand{\statval}{X}  % Statistical value of a random variable
\newcommand{\statdomain}{\mathcal{X}}  % Domain of observations
\newcommand{\statprob}{P}  % Probability distribution
\newcommand{\minimax}{\mathfrak{M}}  % Minimax symbol
\newcommand{\pclass}{\mc{P}}  % Family of probability distributions
\newcommand{\metric}{\rho}  % Metric for errors

\newcommand{\bindic}[1]{\mathbb{I}_{#1}}  % +\infty-valued indicator
\newcommand{\onevec}{\mathbbm{1}}
\newcommand{\zeros}{\mathbbm{0}}
\newcommand{\ones}{\onevec}

% Distributional families
\newcommand{\normal}{\mathsf{N}}  % Normal distribution
\newcommand{\normfam}{\mc{N}}  % Family of normals
\newcommand{\stddev}{\sigma}  % Standard deviation
\newcommand{\bernoulli}{\mathop{\mathrm{Bernoulli}}}
\newcommand{\uniform}{\text{Uniform}}  % uniform distribution
\newcommand{\laplace}{\text{Laplace}}  % Laplace distribution
\newcommand{\expdist}{\mathsf{Exp}}  % Exponential distribution

% Convergence things

\newcommand{\ext}{\mathop{\rm ext}}
\newcommand{\epi}{\mathop{\rm epi}}
\newcommand{\cepi}{\stackrel{\rm epi}{\rightarrow}}
\newcommand{\limext}{\mathop{\rm lim\hspace{1pt}ext}}
\newcommand{\limint}{\mathop{\rm lim\hspace{1pt}int}}
\newcommand{\Nsub}{\mc{N}^{\mathsf{sub}}_\infty}
\newcommand{\Nallsub}{\mc{N}_\infty}
\newcommand{\esssup}{\mathop{\rm ess\hspace{1.8pt}sup}}

% Notation for stochastic optimization problem
\newcommand{\optvar}{\theta} % optimization variable
\newcommand{\fr}{\Theta} % feasible region
\newcommand{\dual}{\mc{L}(\fclass)}
\newcommand{\dualp}{\mc{L}^\infty(\fclass')}
\newcommand{\dualh}{\mathcal{L}(\hclass)}
\newcommand{\dualbdd}{\mc{L}^\infty(\mc{H})}
% \newcommand{\dualbdd}{\ell^\infty(\mc{H})}
\newcommand{\duallin}{\mc{L}_{lin}^\infty(\mc{H})}
\newcommand{\sigalg}{\mathcal{A}}
\newcommand{\hclass}{\mathcal{H}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%% Notation for VarReg Paper %%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{\subg}{\sigma}
\newcommand{\phidiv}[2]{D_{\phi}\left({#1} |\!| {#2}\right)}
\newcommand{\phidivs}[2]{D_{2}({#1} |\!| {#2})}
\newcommand{\phiconp}{{\phi^*}'}
\newcommand{\littleprob}{q_\star}
\newcommand{\littlestprob}{q_{\star\star}}
\newcommand{\zbound}{M}
\newcommand{\maxevent}{\event_{n,\max}}
\newcommand{\varratio}{\mathsf{r}_\star}

\newcommand{\covnum}{N}
\newcommand{\linfstatnorm}[1]{\left\|{#1}\right\|_{L^\infty(\statdomain)}}
\newcommand{\ltwopn}[1]{\left\|{#1}\right\|_{L^2(P_n)}}
\newcommand{\lonestatnorm}[1]{\left\|{#1}\right\|_{L^1}}
\newcommand{\ltwostatnorm}[1]{\left\|{#1}\right\|_{L^2}}
\newcommand{\vcdim}{\mathsf{VC}}
\newcommand{\xprime}{\left(t + \log\ceil{\log\frac{n}{t}}\right)}

\newcommand{\radcomp}{\mathfrak{R}}
\newcommand{\rad}{\radcomp_n(\fclass)}
\newcommand{\worstrad}{\mathfrak{R}^{\rm sup}_n}

\newcommand{\lipobj}{L}
\newcommand{\liphess}{H}
\newcommand{\ball}{\mathbb{B}}
\newcommand{\solset}{S_\star}
\newcommand{\empsolset}{\what{S}_\star}

% bounded-expansions.tex
\newcommand{\stdevthresh}{\tau}

% optimization-selection.tex
\newcommand{\ermsol}{\what{\theta}^{\rm \hspace{1pt} erm}_n}
\newcommand{\robsol}{\what{\theta}^{\rm \hspace{1pt} rob}_n}
\newcommand{\binomial}{\mathsf{Bin}}
\renewcommand{\vcdim}{\mathsf{VC}}
\newcommand{\hnorm}[1]{\norm{#1}_{\mc{H}}}

% not-that-bad.tex
\newcommand{\lipobjsubg}{M}
\newcommand{\growthpow}{\gamma}
\newcommand{\hoodrad}{r}

% experiments.tex
% \providecommand{\robsol}{\what{\theta}^{\rm rob}}
% \providecommand{\ermsol}{\what{\theta}^{\rm erm}}
\newcommand{\radius}{r}

% main-proofs.tex
\newcommand{\exactevent}{\event_{n, {\rm exact}}}

% appendices.tex
\newcommand{\ilow}{i_{\rm low}}
\newcommand{\ihigh}{i_{\rm high}}
\newcommand{\algbox}[2]{
  \begin{center}
    \begin{tabular}{c}
      \fcolorbox{black}{white}{
      \begin{minipage}{.8\textwidth}
        \textbf{Inputs:} {#1}
      \end{minipage}
      } \\
      \fcolorbox{black}{white}{
      \begin{minipage}{.8\textwidth}
        {#2}
      \end{minipage}
      }
    \end{tabular}
  \end{center}
}

%%% New version of \caption puts things in smaller type, single-spaced 
%%% and indents them to set them off more from the text.
\makeatletter
\long\def\@makecaption#1#2{
  \vskip 0.8ex
  \setbox\@tempboxa\hbox{\small {\bf #1:} #2}
  \parindent 1.5em  %% How can we use the global value of this???
  \dimen0=\hsize
  \advance\dimen0 by -3em
  \ifdim \wd\@tempboxa >\dimen0
  \hbox to \hsize{
    \parindent 0em
    \hfil 
    \parbox{\dimen0}{\def\baselinestretch{0.96}\small
      {\bf #1.} #2
      %%\unhbox\@tempboxa
    } 
    \hfil}
  \else \hbox to \hsize{\hfil \box\@tempboxa \hfil}
  \fi
}
\makeatother


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%% Notation for RobustStochastic Paper %%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\newcommand{\uppereta}[1]{u(#1)}
\newcommand{\lowereta}[1]{l(#1)}
\newcommand{\param}{\theta}
\newcommand{\paramdomain}{\Theta}
\newcommand{\modeldomain}{\Theta}

\newcommand{\measures}{\mathcal{M}}
\newcommand{\empdual}{\what{g}_{k,n}}

\newcommand{\modulus}{\mathbb{G}_n}
\newcommand{\kspace}{\ell^{\infty}(\{h: \norm{h} \le K\})}

\newcommand{\hone}{\frac{h_1}{\sqrt{n}}}
\newcommand{\htwo}{\frac{h_2}{\sqrt{n}}}

% modulus process
\newcommand{\modproc}{\mathbb{G}_n}
\newcommand{\modprocone}{\mathbb{G}_{n, 1}}
\newcommand{\modproctwo}{\mathbb{G}_{n, 2}}

\renewcommand{\bindic}[1]{\mathbf{I}\left({#1}\right)}

\newcommand{\distopt}[3]{\mbox{d}_{\rm opt}(#1, #2; #3)}
\providecommand{\noise}{\varepsilon}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%% Notation for Adaptive Experimentation %%%%%%%%%%%%%%%%%%%%%%$
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{\numarm}{K}
\newcommand{\horizon}{T}
\newcommand{\bsr}{\textup{BSR}}
\newcommand{\algofull}{\mathsf{Residual~Horizon~Optimization}}
\newcommand{\algo}{\mathsf{RHO}}
\newcommand{\Pstate}{\sqrt{n}\bar{R}^{n}}
\newcommand{\pstate}{r}
\newcommand{\proj}{P}
\newcommand{\projeps}{P_{\epsilon}}
\newcommand{\projepsc}{P_{\epsilon}^{c}}
