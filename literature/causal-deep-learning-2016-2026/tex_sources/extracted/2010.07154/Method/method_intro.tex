In this section, we formulate our proposed method, {\it deep feature instrumental variable regression} (DFIV).% We first introduce an approach to learn adaptive features in the standard supervised setting, and then apply it to construct our two-stage DFIV algorithm. 